import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'africa.atoria.webile',
  appName: 'webile',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
